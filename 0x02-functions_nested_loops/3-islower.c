#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>


/**
 * print_alphabet_x10 - Entry point of the program
 *
 * Return: 1 (Success)
 */
int _islower(int c)
{
	if(islower(c))
		return (1);
	else
		return (0);
}
